server:
  port: 8080

spring:
  application:
    name: notification-service
  profiles:
    active: local
  jackson:
    default-property-inclusion: non_null

springdoc:
  api-docs:
    path: /api/v1/notifications/api-docs
