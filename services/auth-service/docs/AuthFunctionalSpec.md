# Auth 기능 명세서

> auth-service의 상세 기능 명세입니다. 서비스 간 흐름·책임 경계는 루트 `docs/PRD.md`, 공통 규칙(에러코드/보안 등)은 `.claude/rules/`를 참고하세요.
> 출처: auth-service 기능 정의 시트(AUTH-001~014)

## 범위

이 문서는 auth-service가 담당하는 **로그인·회원가입(계정 생성)·토큰 발급/재발급·비밀번호 재설정·관련 배치**를 다룹니다. 회원 프로필, 구매자/판매자 모드 전환, 찜/팔로우/배송지는 member-service 담당이라 이 문서에 포함하지 않습니다(`PRD.md` 2. 도메인/서비스 개요 기준).

## 기능 목록

### 로그인

#### AUTH-001. 일반 로그인
| 항목 | 내용 |
|---|---|
| PRD 코드 | FL_C_ME_02_01 |
| 권한 | 비로그인 |
| 우선순위 | **MVP** |
| 트리거 | API 호출 |

- **요구사항**: 이메일/비밀번호 검증 후 로그인을 처리하고 토큰을 발급한다.
- **처리 내용**: auth-service 자체 `accounts` 테이블 조회(비밀번호 해시 비교) 후 JWT Access Token 발급, Refresh Token은 httpOnly 쿠키로 발급. `must_change_password=true`인 경우 응답에 플래그 포함.
- **입력값**: 이메일, 비밀번호
- **출력값**: Access/Refresh Token, 회원 기본정보
- **예외 처리**: 불일치 → 401 / 5회 연속 실패 → 30분 자동 계정 잠금(`locked_until`), 셀프 해제는 P2
- **보안/권한** (S1·S2·S3·S9·S10·V12): 서버 측 인증 수행 · 비밀번호는 솔트+해시 비교(S3) · 5회 초과 실패 시 잠금(S3·V12) · 인증정보 HTTPS 전송(S9) · 응답에 비밀번호·해시 미포함, 실패 사유는 통일된 메시지로 계정 존재 여부 비노출(S10)

#### AUTH-002. 소셜 로그인
| 항목 | 내용 |
|---|---|
| PRD 코드 | FL_C_ME_02_01 |
| 권한 | 비로그인 |
| 우선순위 | P1 |
| 트리거 | API 호출 |

- **요구사항**: Google/Kakao 계정으로 기존 회원의 로그인을 처리한다.
- **처리 내용**: OAuth 인가코드로 식별 후 연동된 기존 계정 조회, 토큰 발급
- **입력값**: 소셜 제공자, 인가코드
- **출력값**: Access/Refresh Token, 회원정보
- **예외 처리**: 연동 계정 없음 → 회원가입 유도 / 제공자 응답 실패 → 503
- **보안/권한** (S3·S7·S9·S10): 소셜 제공자 응답값 검증 후 세션 발급(S7) · 토큰 저장·전송 암호화(S9) · 실패 시 일반 메시지(S10)

#### AUTH-003. Access Token 재발급
| 항목 | 내용 |
|---|---|
| PRD 코드 | FL_C_ME_02_01 |
| 권한 | 공통(로그인) |
| 우선순위 | **MVP** |
| 트리거 | API 호출 |

- **요구사항**: 자동로그인 유지를 위해 httpOnly 쿠키의 Refresh Token으로 Access Token을 재발급한다.
- **처리 내용**: 쿠키에서 Refresh Token 추출 → `refresh_tokens` 테이블에서 `DELETE...RETURNING`으로 원자적 확인+폐기 후 신규 토큰 INSERT+Access Token 발급(회전). 반환 행 없으면 재사용 탐지로 해당 계정 전체 세션 강제 로그아웃
- **입력값**: 없음(refreshToken 쿠키 자동 첨부)
- **출력값**: 신규 Access Token(정책에 따라 Refresh 회전)
- **예외 처리**: 쿠키 없음/만료/위조 → 401 → 재로그인 유도 / 폐기된(이미 회전된) 토큰 재사용 감지 시 해당 계정 전체 세션 강제 로그아웃
- **보안/권한** (S1·S3·S9): `DELETE...RETURNING`은 바인딩 변수 사용(S1) · Refresh Token은 httpOnly·Secure·SameSite=Strict 쿠키, Path를 재발급 엔드포인트로 제한(S9) · 위변조 토큰 즉시 거부, 재사용 탐지 시 전체 세션 무효화(S3)

### 회원가입

#### AUTH-004. (폐기) 휴대폰 인증번호 발송

> **2026-08-31 폐기**: 통합인증(PortOne/KG이니시스)으로 벤더가 확정되면서 서버가 인증번호를 직접 발송·관리할 필요가 없어졌다. 클라이언트가 PortOne JS SDK로 인증창(카카오/네이버/PASS/토스/금융인증서 등 민간인증서)을 직접 열고 인증을 완료하므로, 이 엔드포인트는 만들지 않는다. 서버 역할은 AUTH-005 하나로 통합됐다.

#### AUTH-005. 본인인증(PortOne 통합인증) 결과 조회
| 항목 | 내용 |
|---|---|
| PRD 코드 | FL_C_ME_01_02 |
| 권한 | 비로그인 |
| 우선순위 | **MVP** |
| 트리거 | API 호출 |

- **요구사항**: 클라이언트에서 PortOne SDK로 완료한 본인인증 건을 서버가 PortOne REST API로 조회·검증하고, 본인인증 완료 임시 토큰을 발급한다.
- **처리 내용**: `identityVerificationId`로 PortOne 단건조회 API(`GET /identity-verifications/{id}?storeId={PG_STORE_ID}`) 호출(`storeId`를 명시해 다른 상점의 인증 건이 조회되지 않도록 함) → `status`가 `VERIFIED`인지 확인 → `verifiedCustomer`(name/phoneNumber/birthDate)를 Redis에 TTL 30분으로 임시 저장하고 `verificationToken`(UUID) 발급
- **입력값**: `identityVerificationId`
- **출력값**: 본인인증 완료 임시 토큰(`verificationToken`), 만료 시각(`expiresAt`)
- **예외 처리**: 미검증(`status != VERIFIED`) → 401 `TOKEN_INVALID` / PortOne API 호출 실패(네트워크·5xx) → 503 `DEPENDENCY_FAILURE`
- **보안/권한** (S1·S2·S7·S9·S10): 서버 검증·바인딩(S1·S2) · PortOne API 시크릿(`PG_APIKEY`)은 코드와 분리해 환경변수로 보관(S7) · PortOne 응답값(verifiedCustomer)은 검증 후 사용, 신뢰하지 않음(S7) · Redis 저장 인증정보는 TTL 만료 후 자동 폐기, 1회 소비 후 즉시 삭제(S9) · 실패 사유는 상세 노출 없이 일반 메시지(S10)

#### AUTH-006. 이메일 중복 확인
| 항목 | 내용 |
|---|---|
| PRD 코드 | FL_C_ME_01_03 |
| 권한 | 비로그인 |
| 우선순위 | **MVP** |
| 트리거 | API 호출 |

- **요구사항**: 입력한 이메일의 사용 가능 여부를 실시간으로 확인한다.
- **처리 내용**: unique 제약 기준 존재 여부 조회
- **입력값**: 이메일
- **출력값**: 사용 가능 여부(bool)
- **예외 처리**: 형식 오류 → 400 `INVALID_INPUT`
- **보안/권한** (S1·S2·S10): 입력값 서버 검증 및 바인딩 변수 사용(S1·S2) · 오류는 일반 메시지로 응답(S10)

#### AUTH-007. 계정(자격증명) 생성 — 일반가입
| 항목 | 내용 |
|---|---|
| PRD 코드 | FL_C_ME_01_03 |
| 권한 | 비로그인 |
| 우선순위 | **MVP** |
| 트리거 | API 호출 |

- **요구사항**: 아이디/비밀번호 기반 계정을 생성하고, 프로필 생성을 위해 member-service를 호출한다.
- **처리 내용**: 이메일 중복 검증 → 비밀번호 BCrypt 해시 저장(`accounts` 테이블) → 본인인증 임시토큰(`verificationToken`) 검증(AUTH-005에서 발급된 토큰을 Redis에서 1회 소비하고, 저장된 휴대폰번호와 요청의 휴대폰번호가 일치하는지 대조) → member-service에 프로필 생성 동기 호출 — 실패 시 보상 트랜잭션(계정 삭제)으로 처리, AUTH-012가 안전망
- **입력값**: 비밀번호, 이메일, 본인인증 임시토큰, (프로필용) 이름, 휴대전화번호, 약관동의 목록, (선택) 주소
- **출력값**: 생성된 accountId, 가입 완료 회원정보(memberId 포함), 초기 Access/Refresh Token
- **예외 처리**: 필수값 누락/형식오류 → 400 / 이메일 중복 → 409 / 본인인증 임시토큰 만료·미존재·휴대폰번호 불일치 → 401 `TOKEN_INVALID` / member-service 프로필 생성 실패 → 보상 트랜잭션으로 계정 삭제 후 503
- **보안/권한** (S1·S2·S3·S9·S10): 비밀번호는 솔트 포함 해시로 저장, 복잡도 검증(S3) · 전 입력값 서버 검증·바인딩(S1·S2) · 이메일 등 저장 시 암호화, HTTPS 전송(S9) · 비밀번호 평문 로깅 금지(S10)

#### AUTH-008. 계정(자격증명) 생성 — 소셜가입
| 항목 | 내용 |
|---|---|
| PRD 코드 | FL_C_ME_01_03 |
| 권한 | 비로그인 |
| 우선순위 | P1 |
| 트리거 | API 호출 |

- **요구사항**: Google/Kakao 인가코드로 소셜 계정을 생성하고, 프로필 생성을 위해 member-service를 호출한다.
- **처리 내용**: OAuth 인가코드 또는 signupToken(소셜 로그인 시 미가입 판정되며 발급)으로 제공자 사용자정보 확보 → 기존 연동계정 확인 → 신규 계정 생성 → member-service에 프로필 생성 동기 호출 — 실패 시 보상 트랜잭션
- **입력값**: 소셜 제공자, 인가코드, 약관동의 목록, (부족시) 추가정보
- **출력값**: 생성된 accountId, 가입 완료 회원정보, 초기 토큰
- **예외 처리**: 이미 가입된 계정 → 로그인 안내 / 제공자 응답 실패 → 503 / 프로필 생성 실패 → 보상 트랜잭션으로 계정 삭제
- **보안/권한** (S1·S2·S7·S9·S10): 소셜 인가코드 검증 및 제공자 응답값 신뢰 전 검증(S7) · 수신 개인정보·토큰 코드 하드코딩 금지, 저장 시 암호화(S9) · 입력 검증·바인딩(S1·S2) · 실패 시 일반 메시지(S10)

#### AUTH-009. 이메일 찾기
| 항목 | 내용 |
|---|---|
| PRD 코드 | FL_C_ME_02_01 |
| 권한 | 비로그인 |
| 우선순위 | P1 |
| 트리거 | API 호출 |

- **요구사항**: 본인인증한 휴대폰번호로 가입된 이메일을 안내한다.
- **처리 내용**: 본인인증 완료 후, `accounts.id`와 member-service PK가 같은 UUID를 공유하는 설계를 활용해 조회한다 — ① member-service에 휴대폰번호로 계정 ID 조회 → ② 반환된 ID로 auth-service 자체 `accounts` 테이블에서 이메일 조회. 구체적 API 계약은 member-service 쪽 문서와 맞춰 확정 필요.
- **입력값**: 휴대폰번호, 본인인증 임시토큰
- **출력값**: 마스킹된 이메일
- **예외 처리**: 가입 계정 없음 → **에러코드 없음, 의도적으로 항상 성공 형태로 응답** (존재 여부 직접 노출 안 되게 문구 검토)
- **보안/권한** (S9·S10·V13): 계정 존재 여부 자체가 개인정보이므로 가입 여부를 직접 노출하지 않는 응답 문구 사용(S10·V13) · 마스킹된 아이디만 SMS로 전달, 화면 미노출(S9)

#### AUTH-010. 비밀번호 재설정 링크 발송
| 항목 | 내용 |
|---|---|
| PRD 코드 | FL_C_ME_02_01 |
| 권한 | 비로그인 |
| 우선순위 | P1 |
| 트리거 | API 호출 |

- **요구사항**: 본인인증한 정보로 임시 비밀번호를 발급하고 SMS로 안내한다. *(처리 내용은 이메일 발송으로 되어 있어 요구사항 문구와 살짝 다름 — 아래 참고)*
- **처리 내용**: 이메일로 계정 조회 → 재설정 토큰 생성(단기 만료·1회용, `password_reset_tokens` 테이블 신규 필요 — `refresh_tokens`와 동일 패턴) → 재설정 링크(프론트 URL+토큰) 이메일 발송
    - **확정**: SMS 본인인증 단계 없이 이메일 소유 확인만으로 진행한다(MVP). 필요해지면 휴대폰 인증 단계를 나중에 추가할 수 있는 구조라 지금 막을 이유는 없음.
- **입력값**: 이메일
- **출력값**: 발송 결과 메시지
- **예외 처리**: 계정 없음 → **에러코드 없음, 존재여부 노출 방지 위해 동일 메시지로 응답** / 이메일 발송 실패 → 503
- **보안/권한** (S7·S9·S10·V13): 계정 존재 여부 비노출(S10·V13) · 재설정 토큰은 충분히 무작위·단기 만료·1회용으로 생성(S9) · 이메일 발송 연동 API Key는 코드와 분리 보관(S7)

#### AUTH-011. 비밀번호 변경 (마이페이지, 로그인 상태)
| 항목 | 내용 |
|---|---|
| PRD 코드 | — |
| 권한 | 공통(로그인) |
| 우선순위 | **MVP** |
| 트리거 | API 호출 |

- **요구사항**: 임시 비밀번호로 로그인한 회원이 새 비밀번호로 변경한다.
- **처리 내용**: 현재(임시) 비밀번호 검증 후 새 비밀번호 해시 저장, `must_change_password=false`로 갱신
- **입력값**: currentPassword, newPassword
- **출력값**: 처리 결과
- **예외 처리**: 현재 비밀번호 불일치 → 401
- **보안/권한** (S3·S9): 새 비밀번호도 솔트 포함 해시 저장(S3) · 평문 로깅 금지(S9)

#### AUTH-012. 고아 계정 정리 배치
| 항목 | 내용 |
|---|---|
| PRD 코드 | — |
| 권한 | 시스템 |
| 우선순위 | P2 |
| 트리거 | 스케줄러(배치) |

- **요구사항**: 회원가입 보상 트랜잭션이 실패해 프로필 없이 남은 계정을 주기적으로 정리한다.
- **처리 내용**: 일정 시간(예: 10분) 경과 후에도 대응 프로필이 없는 계정 조회
    - **[의도적 보류, 2026.08.26]** 처리 방식(자동삭제 vs 운영알림 후 수동확인)은 실제 발생 사례 관찰 후 결정하기로 보류됨
- **입력값**: (배치 트리거)
- **출력값**: 정리 결과(대상 건수)
- **예외 처리**: —
- **보안/권한** (S1): 배치 쿼리 바인딩 변수 사용

#### AUTH-013. 만료된 Refresh Token 정리 배치
| 항목 | 내용 |
|---|---|
| PRD 코드 | — |
| 권한 | 시스템 |
| 우선순위 | P2 |
| 트리거 | 스케줄러(배치) |

- **요구사항**: 재발급 없이 그냥 만료된 `refresh_tokens` 행을 주기적으로 정리한다.
- **처리 내용**: 1일 1회 `expires_at < now()`인 행 DELETE
- **입력값**: (배치 트리거)
- **출력값**: 정리 결과(대상 건수)
- **예외 처리**: —
- **보안/권한** (S1): 배치 쿼리 바인딩 변수 사용

#### AUTH-014. 재설정 토큰으로 비밀번호 설정
| 항목 | 내용 |
|---|---|
| PRD 코드 | — |
| 권한 | 비로그인(토큰이 인증 수단) |
| 우선순위 | P1 |
| 트리거 | API 호출 |

- **요구사항**: 이메일로 받은 재설정 링크의 토큰을 검증해 새 비밀번호를 설정한다.
- **처리 내용**: 토큰 존재·미만료·미사용 확인 → 새 비밀번호 해시 저장 → 토큰 즉시 폐기(1회용)
- **입력값**: resetToken, newPassword
- **출력값**: 처리 결과
- **예외 처리**: 토큰 없음/만료/이미 사용됨 → 401 → 재발급 요청 유도
- **보안/권한** (S3·S9): 새 비밀번호 솔트 포함 해시 저장(S3) · 토큰은 1회용, 사용 즉시 폐기하여 재사용 방지(S9)

---

## 에러 코드 매핑

`error-handling.md` 기준으로, 공통 코드는 `CommonErrorCode`를 그대로 쓰고 새 코드만 `AuthErrorCode`(도메인 전용, `implements ErrorCode`)에 추가합니다.

| 상황 | 코드 | 관련 항목 |
|---|---|---|
| 로그인 이메일/비밀번호 불일치 | `AuthErrorCode.INVALID_CREDENTIALS` (신규, 401) | AUTH-001, AUTH-011 |
| 계정 잠금(5회 로그인 실패) | `AuthErrorCode.ACCOUNT_LOCKED` (신규, 423 권장) | AUTH-001 |
| 소셜 로그인 미연동 계정 | 에러 아님 — 회원가입 유도 응답으로 구분 | AUTH-002 |
| 소셜 로그인/이메일 발송/PortOne 연동 등 외부 연동 실패 | `CommonErrorCode.DEPENDENCY_FAILURE`(503, 기존) | AUTH-002, AUTH-005, AUTH-007, AUTH-008, AUTH-010 |
| Refresh Token 없음/만료/위조/재사용 | `CommonErrorCode.TOKEN_INVALID` / `TOKEN_EXPIRED`(401, 기존) | AUTH-003, AUTH-014 |
| PortOne 본인인증 미검증(`status != VERIFIED`) | `CommonErrorCode.TOKEN_INVALID`(401, 기존) | AUTH-005 |
| 본인인증 임시토큰 만료·미존재·휴대폰번호 불일치 | `CommonErrorCode.TOKEN_INVALID`(401, 기존) | AUTH-007 |
| 이메일 형식 오류 / 필수값 누락 | `CommonErrorCode.INVALID_INPUT`(400, 기존) | AUTH-006, AUTH-007, AUTH-008 |
| 이메일 중복 | `AuthErrorCode.EMAIL_ALREADY_EXISTS` (신규, 409) | AUTH-006, AUTH-007 |
| 이미 가입된 소셜 계정 | `AuthErrorCode.EMAIL_ALREADY_EXISTS` 재사용 또는 `SOCIAL_ACCOUNT_ALREADY_EXISTS`(신규) | AUTH-008 |
| 이메일 찾기/재설정 링크 발송 시 계정 없음 | **에러코드 없음** — 존재 여부 비노출 위해 항상 성공 형태로 응답 (AUTH-009, AUTH-010 참고) | AUTH-009, AUTH-010 |

---

## 미확정/보류 사항 (한눈에 보기)

| 위치 | 내용 | 상태 |
|---|---|---|
| 문서 상단 | 본인인증 담당 서비스 — PRD.md 오류로 확인됨 | ✅ 확인됨, PRD.md 수정은 사용자가 별도 진행 |
| 문서 상단 | AUTH-001 "member-service의 계정정보 조회" 문구 — 갱신 안 된 예전 정보였음 | ✅ 해결됨 |
| 문서 상단 | `V12`/`V13` 보안 코드 — `security.md` S3/S10과 동일 내용으로 확인, 상호 참조 추가 (+ S8 CSRF 누락분 발견해 추가) | ✅ 해결됨 |
| AUTH-009 | `accounts`에 휴대폰번호 컬럼 없음 → 공유 UUID 활용(member-service 조회 후 자체 테이블 조회)으로 확정 | ✅ 해결됨, API 계약은 member-service 문서와 맞춰 확정 필요 |
| 스키마 | `role` 값 — `member`/`admin`으로 확정, 기존 DDL(`USER`/`ADMIN`)은 사용자가 직접 수정 예정 | ✅ 해결됨 |
| AUTH-010 | SMS 본인인증 없이 이메일 소유 확인만으로 재설정 진행 | ✅ 확정(MVP), 필요 시 추후 휴대폰 인증 단계 추가 가능 |
| AUTH-012 | 고아 계정 정리 방식(자동삭제 vs 수동확인) | 의도적 보류(2026.08.26) |